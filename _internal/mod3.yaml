# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/mod3  # dataset root dir
train: M:/imgFase3/train  # train images (relative to 'path') 128 images
val: M:/imgFase3/val  # val images (relative to 'path') 128 images
test: M:/imgFase3/test # test images (optional)

# Clases
names:
  0: hito
